package engine;

/**
 * Represents the Sprite the view is to use to render a engine.GameObject
 */
public class Sprite {

    private int spriteIndex;

    /**
     * Returns a index number the view uses to identify the sprite to render.
     * @return int
     */
    public int getSpriteIndex() {
        return spriteIndex;
    }
}
